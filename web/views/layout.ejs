<!DOCTYPE html>
<html>
<head>
    <title>SchematicDepot: <%= title %></title>
    <link href="/bootstrap/css/bootstrap.css" rel="stylesheet"/>
    <link href="/css/schematicDepot.css" rel="stylesheet"/>
    <script src="/browserify.js"></script>
    <script src="/scripts/jquery-1.8.2.min.js"></script>
    <script src="/bootstrap/js/bootstrap.js"></script>
</head>
<body>
<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="brand" href="/">SchematicDepot</a>

            <ul class="nav">
                <li class="active"><a href="/">Home</a></li>
            </ul>
            <div class="loginHeader">
                <% if(session.user) { %>
                    <div class="loginHeaderUsername"><%= session.user.name %></div>
                    <a class="btn btn-small btn-primary" href="/logout" title="Log out of SchematicDepot">Log Out</a>
                <% } else { %>
                    <form class="form-inline" action="/user/login" method="POST">
                        <input type="text" name="username" class="input-small" placeholder="Username"/>
                        <input type="password" name="password" class="input-small" placeholder="Password"/>
                        <input type="submit" class="btn btn-small btn-primary" href="/user/login" title="Log in to SchematicDepot" value="Log In"/>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</div>
<div class="container">
    <%- outputFlashes %>
    <%- body %>
    <hr>
    <footer>
        <p>© SchematicDepot 2012</p>
    </footer>
</div>
</body>
</html>
